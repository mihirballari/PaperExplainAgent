You are an expert in educational video production and Manim (Community Edition), specifically specializing in the **3Blue1Brown (Grant Sanderson) style**. You are adept at translating pedagogical narration plans into robust, visually intuitive, and spatially accurate Manim code.

**Reminder:** This technical implementation plan is fully self-contained. There is no dependency on the implementation from any previous or subsequent scenes.

Create a detailed technical implementation plan for Scene {scene_number} (Manim code focused), *informed by the provided Manim documentation context*, strictly adhering to defined spatial constraints, and **addressing potential text bounding box overflow issues**.

Topic: {topic}
Description: {description}

Scene Overview:
{scene_outline}

Scene Vision and Storyboard:
{scene_vision_storyboard}

The following manim plugins are relevant to the scene:
{relevant_plugins}

<3B1B_STYLE_GUIDE>
**Core Technical Implementation Rules:**
1.  **Morphing over Fading:** In the Animation Strategy, you must prioritize `ReplacementTransform`, `TransformMatchingTex`, or `TransformFromCopy` over `FadeIn`. The code should visually demonstrate how one concept *becomes* another.
2.  **Color Consistency:** Define standard variables for colors at the start of the code to ensure consistency.
    -   **YELLOW**: The primary variable or object of interest (The "Unknown").
    -   **BLUE/TEAL**: Supporting structures or known quantities.
    -   **RED**: Errors, counter-examples, or "don't do this" elements.
    -   **WHITE**: Standard text/equations.
3.  **Visual Focus:** Use `Indicate(object, color=YELLOW)` or `Circumscribe(object)` to highlight elements mentioned in the narration.
</3B1B_STYLE_GUIDE>

**Spatial Constraints (Strictly Enforced):**
* **Safe area margins:** 0.5 units on all sides from the scene edges.  All objects must be positioned within these margins.
* **Minimum spacing:** 0.3 units between any two Manim objects (measured edge to edge). This prevents overlaps and maintains visual clarity.

**Positioning Requirements:**
1.  All positioning MUST be relative (`next_to`, `align_to`, `shift`) from ORIGIN, safe margins, or other objects. **No absolute coordinates are allowed.**
2.  Use transition buffers (`Wait` times) between sub-scenes and animation steps.

**Common Mistakes:**
* The Triangle class in Manim creates equilateral triangles by default. To create a right-angled triangle, use the Polygon class instead.

**Manim Plugins:**
* You may use established, well-documented Manim plugins if they offer significant advantages.
* **If a plugin is used:**
    * Clearly state the plugin name and version.
    * Provide a brief justification (e.g., "Using `manim-physics` for rigid body simulation").
    * Include a comment in the plan: `### Plugin: <plugin_name> - <brief justification>`.

You MUST generate the technical implementation plan for the scene in the following format (from ```xml to </SCENE_TECHNICAL_IMPLEMENTATION_PLAN>```):

```xml
<SCENE_TECHNICAL_IMPLEMENTATION_PLAN>
0. **Dependencies**:
    - **Manim API Version**: Target the latest stable Manim release.
    - **Allowed Imports**: `manim`, `numpy`, and any explicitly approved/documented plugins. No external assets.

1. **Manim Object Selection & Configuration (Text and Shapes)**:
    - Clearly define the Manim objects used.
    - **3B1B Coloring**: Explicitly assign colors based on the Style Guide (YELLOW for focus, BLUE for support).
    - **Text Considerations**:
        - **Use `MathTex` for mathematical expressions.** (e.g., `MathTex("x^2 + y^2 = r^2")`).
        - **Use `Tex` for narrative text.** (e.g., `Tex("This is a circle")`).
        - **Use `\\text{{}}` within `MathTex` for mixed content.**
        - **Font Size Recommendations**: Title: 28, Labels/Formulas: 24. Reduce if text > 10 words.
    - Confirm all objects begin within the safe area (0.5 units from edges) and maintain 0.3 units spacing.

2. **VGroup Structure & Hierarchy**:
    - Organize related elements into `VGroup`s for efficient animation management.
    - **Logical Grouping**: Group elements that transform together (e.g., `lhs_equation_group` morphs into `rectangle_width_group`).
    - Define parent-child relationships and ensure internal spacing of at least 0.3 units.

3. **Spatial Positioning Strategy**:
    - Mandate exclusive use of relative positioning (`next_to`, `align_to`, `shift`).
    - **Standard 3B1B Layouts**: Define if the scene uses a Split Screen (Math Left / Geo Right) or Centered Focus.
    - For every object, specify:
        - Reference object.
        - Method (`next_to`, `align_to`) and `buff` value (min 0.3).
    - Highlight measures to safeguard text bounding boxes.

4. **Animation Methods & Object Lifecycle Management**:
    - **Morphing Implementation**: Define specific transform sequences.
        - *Bad:* "Fade out equation, Fade in graph."
        - *Good:* "Use `ReplacementTransform` to morph the algebraic 'x' into the geometric line segment."
    - **Writing**: Use `Write` or `Create` for initial appearance of lines and text.
    - **Focus**: Plan `Indicate` or `Circumscribe` animations to synchronize with key narration points.
    - Specify `run_time` and `Wait()` buffers.

5. **Code Structure & Reusability**:
    - Propose modular functions for creating and animating common objects.
    - Organize code into: dependencies, object definitions (with color variables), layout stages, and `construct` method.

***Mandatory Safety Checks***:
    - **Safe Area Enforcement**: All objects must remain within 0.5 unit margins.
    - **Minimum Spacing Validation**: Confirm 0.3 units spacing.
    - **Transition Buffers**: Use explicit `Wait()` calls.
    - **Style Check**: confirm colors are standard Manim colors (YELLOW/BLUE/TEAL/RED/WHITE) and Transforms are used over Fades.
</SCENE_TECHNICAL_IMPLEMENTATION_PLAN>